<?xml version="1.0" encoding="utf-8"?>
<!-- FILE: AndroidManifest.xml -->
<!-- TÁC DỤNG: Khai báo app ví BTC cho hệ thống Android -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hvl.wallet">

    <!-- QUYỀN INTERNET - bitcoinj cần để đồng bộ blockchain mainnet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        <!-- SỬA LỖI Ở ĐÂY: dùng icon mặc định của hệ thống -->
        <!-- Lần trước trỏ tới @mipmap/ic_launcher nhưng file không tồn tại -->
        android:icon="@android:mipmap/sym_def_app_icon"
        android:roundIcon="@android:mipmap/sym_def_app_icon">

        <!-- Activity chính - màn hình ví -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>